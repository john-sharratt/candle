# Folder metadata for the Zen Code substrate.
#
# The daemon reads this file before it generates anything. Questions listed here
# are used as-is and are NOT regenerated — so hand-written questions win, and
# they survive `--wipe-substrate`.
#
# The `questions` are what makes this folder findable. They are the queries a
# developer would type whose ANSWER is this folder, written by someone who has
# NOT yet found it: no paths, no file names, no "this folder".
#
#   locational   where something lives
#   mechanistic  how something works
#   conceptual   what a name means
#   systemic     the same subject in plain English, no jargon at all
#
# Delete a list to have the daemon regenerate it. `--wipe-metadata` deletes every
# one of these files and starts over.
folder:
  path: candle-kernels/src/delta-net/
  summary: 'The `delta-net` module consists of five CUDA/C++ files: a Rust API layer (`api.rs`) and four kernel implementations (a common header, prefill decoder for 1B/7B models with flash attention/voltron configs, an optimized decode path handling variable KV cache updates via warp-level primitives like shuffles/warpgroups/memcpyasync, plus optional int8 quantization). These components collectively provide the high-performance forward pass logic used by Candle''s delta-net inference engine.'
  distinctive_terms:
  - dn_sigmoid
  - dn_softplus
  - delta_net
  - DnSpan
  - v_new
  - q_scale
  - run_delta_net_conv_prefill_f32
  - DELTA_NET_PREFILL_CHUNK
  - run_delta_net_prefill_state_f32
  - alpha_wave
  - run_delta_net_norm_gate_f32
  - state_out
  - s_tile
  - x_wave
  - trow_in
  - t_len
  - DNP_DIM
  - DNP_THREADS
  - span_start
  - DELTA_NET_PREFILL_DIM
  - DNP_CHUNK
  - DNP_ALD
  - a_base
  - DN_HEAD_DIM
  content_hash: f253c948127db61f367529553f5dc4ffa652664d629620e6bab86ac57fae5f64
questions:
  conceptual:
  - What is the specific purpose of the `delta_net` architecture compared to standard transformers?
  - What does the term `DnSpan` represent in the context of this neural network's internal data structures?
  - How is the scaling factor `q_scale` derived and applied to query vectors?
  - What is the intended behavior of `dn_softplus` as an activation function in this specific model variant?
  - What does `run_delta_net_prefill_state_f32` accomplish that differs from a standard transformer prefill operation?
  - Why is the variable `alpha_wave` used for adaptive normalization in this context?
  - What is the semantic meaning of `state_out` in the recurrent state maintenance of the DeltaNet layer?
  - How does `DELTA_NET_PREFILL_CHUNK` influence the parallelism strategy during the initial processing step?
  - What conceptual role does `DNP_DIM` define regarding the dimensionality of the delta-net layers?
  locational:
  - Where are the CUDA kernels that handle the prefill phase for the DeltaNet models located?
  - Which file contains the implementation of `run_delta_net_conv_prefill_f32`?
  - Where is the `state_out` variable managed during the decoding stage?
  - In which header file are the warp-level primitives like `memcpyasync` utilized for the decode path?
  - Where can I find the definitions for `DNP_DIM` and `DN_HEAD_DIM` used in the kernel configurations?
  - Which source file implements the logic for `alpha_wave` normalization?
  - Where is `DELTA_NET_PREFILL_CHUNK` defined and used within the prefill kernel?
  - What file contains the `deltas` computation logic using `dn_sigmoid` and `dn_softplus`?
  - Where are `s_tile` and `x_wave` initialized for the matrix operations?
  mechanistic:
  - How does `run_delta_net_conv_prefill_c32` utilize `DELTA_NET_PREFILL_DIM` to configure the launch parameters?
  - What role does `dn_sigmoid` play in gating the input transformations before convolution?
  - How is `v_new` computed from `q_scale` and the current key-value cache state?
  - Why is `dn_softplus` applied specifically after the linear transformation in the gate mechanism?
  - How does the decoder kernel use `DNP_ALD` to manage memory alignment for the state update?
  - What is the function of `span_start` when indexing into the context window during prefill?
  - How do `DNP_THREADS` and `DNP_CHUNK` determine the grid size for the prefill kernel execution?
  - In what way does `run_delta_net_norm_gate_f32` adjust the hidden states using `alpha_wave`?
  - How does `trow_in` map to the actual sequence length `t_len` during the attention mask generation?
  systemic:
  - What strategies are employed to handle variable-length sequences efficiently during inference?
  - How does the codebase support mixed-precision or quantized operations alongside standard float32 computations?
  - What mechanisms ensure memory efficiency when managing large key-value caches for long contexts?
  - How is the distinction between prefill and decode phases implemented to optimize latency versus throughput?
  - What design choices were made to maximize occupancy on NVIDIA GPUs for these specific kernels?
  - How does the system synchronize data movement between global memory and shared memory?
  - What approach is taken to minimize overhead when switching between different model sizes, such as 1B and 7B parameters?
  - How are numerical stability issues addressed in the core mathematical operations of the forward pass?
  - What abstraction layer exists between the Rust API and the underlying CUDA kernel implementations?
