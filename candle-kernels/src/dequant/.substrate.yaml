# Folder metadata for the Zen Code substrate.
#
# The daemon reads this file before it generates anything. Questions listed here
# are used as-is and are NOT regenerated — so hand-written questions win, and
# they survive `--wipe-substrate`.
#
# The `questions` are what makes this folder findable. They are the queries a
# developer would type whose ANSWER is this folder, written by someone who has
# NOT yet found it: no paths, no file names, no "this folder".
#
#   locational   where something lives
#   mechanistic  how something works
#   conceptual   what a name means
#   systemic     the same subject in plain English, no jargon at all
#
# Delete a list to have the daemon regenerate it. `--wipe-metadata` deletes every
# one of these files and starts over.
folder:
  path: candle-kernels/src/dequant/
  summary: The `dequant` folder contains 16 CUDA header files implementing various dequantization kernels for different quantization formats (such as Q2_K through Q8K and AWQ variants). These `.cu.h` headers likely contain the device-side logic required to convert low-precision weights back into higher precision during inference.
  distinctive_terms:
  - compute_t
  - nibble_offset
  - nibble1
  - nibble0
  - total_warps
  - QK_K
  - q_2bit
  - neg_ml
  - decode_q3_k_scale
  - ql4
  - val_lo
  - val_hi
  - shift_l
  - shift_h
  - q8a128_store4
  - char4
  - qs_idx
  - h_bit
  - h_lo
  - h_hi
  - pair_idx
  - total_tiles
  - get_scale_min_k4
  - QK8_1
  content_hash: 7a0ac630794ab3c3074a1453566f375494febff18108f0c68658345087b8e6e9
questions:
  conceptual:
  - What is the purpose of the `compute_t` struct or typedef in this context?
  - Why is the variable named `nibble_offset` significant in the context of low-precision weights?
  - What does the term `nibble1` refer to in the packing scheme used here?
  - What does `QK_K` represent regarding the block size or layout of the quantized data?
  - What is the significance of the `q_2bit naming convention for the quantization level?
  - What is the semantic meaning of `neg_ml` in the dequantization formula?
  - What kind of data structure or value does `decode_q3_k_scale` return?
  - What does `ql4` specifically denote in terms of data width or storage unit?
  - What is represented by `val_lo` versus `val_hi` in the reconstruction logic?
  locational:
  - Where are the device-side kernels defined that handle dequantization for various quantization formats like Q2_K through Q8K?
  - Which source files contain the implementation logic for converting low-precision weights back into higher precision during inference?
  - In which files are the variables `compute_t` and `QK_K` declared or used?
  - Where can I find the code that initializes or uses `total_warps`?
  - Which headers implement the specific logic for handling `nibble_offset`, `nibble1`, and `nibble0`?
  - Where are the functions `decode_q3_k_scale` and `get_scale_min_k4` located within the project structure?
  - Are the constants `q_2bit`, `neg_ml`, `ql4`, `val_lo`, and `val_hi` defined in the same set of header files?
  - Where do the bit manipulation operations involving `shift_l` and `shift_h` take place?
  - Which file contains the definition or usage of `q8a128_store4`?
  mechanistic:
  - How is `compute_t` initialized and updated during the dequantization kernel execution?
  - What role does `nibble_offset` play when accessing data from the quantized weight buffer?
  - How are `nibble1` and `nibble0` extracted from the packed weight data?
  - How does `total_warps` determine the grid dimensions for the CUDA launch configuration?
  - What is the step-by-step process for decoding a block using `decode_q3_k_scale"?
  - How are the values `val_lo` and `val_hi` combined to reconstruct the final floating-point output?
  - How is the `q_2bit` format unpacked into its constituent components during runtime?
  - What operation does `neg_ml` perform on the scale factors before they are applied?
  - How does the kernel use `ql4` to extract 4-bit values from the input tensor?
  systemic:
  - How does the system manage memory alignment requirements for different quantization formats like Q2_K, Q4_K, Q5_K, Q6_K, and Q8_K?
  - In what way are multiple quantization schemes (e.g., AWQ vs K-quants) handled within the same codebase structure?
  - How does the project ensure that dequantization kernels correctly handle edge cases such as zero scales or minimums?
  - What is the strategy for distributing work across GPU threads when processing blocks of varying sizes?
  - How are small-bit quantization formats (like 2-bit or 3-bit) efficiently unpacked into standard float32 types without excessive overhead?
  - Does the architecture support mixed-precision inference where different layers use different quantization levels?
  - How are scale and min values stored alongside the compressed weights, and how is their accessibility optimized during runtime?
  - What mechanisms prevent out-of-bounds memory access when extracting nibbles or bytes from packed weight tensors?
  - How scalable is the current design if we were to add a new, non-standard quantization format later on?
