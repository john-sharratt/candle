# Folder metadata for the Zen Code substrate.
#
# The daemon reads this file before it generates anything. Questions listed here
# are used as-is and are NOT regenerated — so hand-written questions win, and
# they survive `--wipe-substrate`.
#
# The `questions` are what makes this folder findable. They are the queries a
# developer would type whose ANSWER is this folder, written by someone who has
# NOT yet found it: no paths, no file names, no "this folder".
#
#   locational   where something lives
#   mechanistic  how something works
#   conceptual   what a name means
#   systemic     the same subject in plain English, no jargon at all
#
# Delete a list to have the daemon regenerate it. `--wipe-metadata` deletes every
# one of these files and starts over.
folder:
  path: candle-transformers/tests/quantized/
  distinctive_terms:
  - cuda_tests
  - get_cuda_device
  - CUDA_TEST_LOCK
  - q4_0_config
  - run_nibble_pair_diagnostic
  - REL_DIFF_THRESHOLD
  - validate_gemx_matmul
  - compute_baseline
  - q5_0_config
  - run_kernel_isolation_diagnostics
  - quick_check
  - run_y_stride_diagnostic_batched
  - negative_tests
  - run_single_column_probe_diagnostic
  - q2k_config
  - get_tolerance_for
  - run_warp_signature_diagnostic
  - create_test_weights
  - run_qh_raw_verification_diagnostic
  - run_k_row_isolation_diagnostic
  - NEAR_ZERO_THRESHOLD
  - RTOL_F8
  - test_nan_detection
  - run_dequant_value_diagnostic
  - run_column_pair_pattern_diagnostic
  - run_column_identity_diagnostic
  - run_dequant_kernel_diagnostic
  - run_position_mapping_diagnostic
  content_hash: 7eaf24770893e21ffaf2bf2f836ef379d25a5284799f02b2a2b37ac1dbb55538
questions:
  conceptual: []
  locational: []
  mechanistic: []
  systemic: []
