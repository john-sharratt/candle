# Folder metadata for the Zen Code substrate.
#
# The daemon reads this file before it generates anything. Questions listed here
# are used as-is and are NOT regenerated — so hand-written questions win, and
# they survive `--wipe-substrate`.
#
# The `questions` are what makes this folder findable. They are the queries a
# developer would type whose ANSWER is this folder, written by someone who has
# NOT yet found it: no paths, no file names, no "this folder".
#
#   locational   where something lives
#   mechanistic  how something works
#   conceptual   what a name means
#   systemic     the same subject in plain English, no jargon at all
#
# Delete a list to have the daemon regenerate it. `--wipe-metadata` deletes every
# one of these files and starts over.
folder:
  path: candle-nn/src/kv_cache/chunked/sampled_selection/
  distinctive_terms:
  - from_backing_owned
  - k_thresholds
  - SampleQuantizationResult
  - normalised_error
  - per_head_table_buf
  - Q0_M2
  - DEFAULT_REPORT_ARENA_CHUNKS
  - PagedSelectionCpuInputs
  - profile_parent_total_ms
  - stage_i32_slice
  - profile_root
  - from_cuda_tag
  - QWEN36_MOE_KV_FACTORS
  - head_gids_buf
  - float_range
  - LLAMA2_KV_FACTOR
  - PALETTE_SLOTS
  - sample_error_surface_kv_paged
  - batch_select_and_summarize
  - max_abs_error
  - PRODUCTION_V_QREL_HIGH_THRESHOLDS
  - sample_quantization_sweep_kv_paged
  - from_backing
  - stage_bytes_as_gpu_buf
  - profile_section_label
  - sample_and_select
  - PRODUCTION_V_QREL_LOW_THRESHOLDS
  - n_k_thresholds
  content_hash: de8c1db7d158a529a4bd97749c061b7db7e84ccf82659fc1636554d633ff3451
questions:
  conceptual: []
  locational: []
  mechanistic: []
  systemic: []
