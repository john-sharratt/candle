# Folder metadata for the Zen Code substrate.
#
# The daemon reads this file before it generates anything. Questions listed here
# are used as-is and are NOT regenerated — so hand-written questions win, and
# they survive `--wipe-substrate`.
#
# The `questions` are what makes this folder findable. They are the queries a
# developer would type whose ANSWER is this folder, written by someone who has
# NOT yet found it: no paths, no file names, no "this folder".
#
#   locational   where something lives
#   mechanistic  how something works
#   conceptual   what a name means
#   systemic     the same subject in plain English, no jargon at all
#
# Delete a list to have the daemon regenerate it. `--wipe-metadata` deletes every
# one of these files and starts over.
folder:
  path: candle-nn/src/kv_cache/chunked/sampled_selection/tests/
  distinctive_terms:
  - pack_r16_blocks
  - r16_dump_path
  - pack_f16
  - dump_path
  - TEST_ARENA_CHUNKS
  - ChunkGpu
  - R16_DUMP_REL_PATH
  - dump_path_for
  - cpu_q_attn_weighted_loss
  - K_MAGWEIGHT_THRESHOLDS
  - select_formats_k_qproj_with_errors
  - BlockFormat
  - K_QPROJ_THRESHOLDS
  - select_best_passing_format
  - compute_q_subspace
  - COSINE_THRESHOLDS
  - select_format_sink_aware
  - cpu_select_k_qproj
  - block_format_from_kv
  - QWEN3_DUMP_REL_PATH
  - q_projected_block_error
  - ComboStats
  - SelectionPassTiming
  - reduce_per_head_bucket_with_dist
  - curve_level_stats
  - ComponentStats
  - LevelResult
  - tag_to_block_format
  content_hash: 360daeef8a8f6f4d8d1a3015e882b8845f483642e859eaaa5bdfdc661f5b4632
questions:
  conceptual: []
  locational: []
  mechanistic: []
  systemic: []
